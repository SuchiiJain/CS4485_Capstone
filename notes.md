- Resarching github hooks, how they work, and what is needed to get them set up
- Researching ngrok as a way to create a webhook URL for my machine
- Set up flask to host a small server locally for communication
- Testing on a personal project repo
- Generated personal access token, expires in 90 days
- Webhook secret: testsecret123
- Brainstorming how to make the notifications more in-your-face rather than just generating a log that the user has to check manually
- set it up so the server can be run with all settings with the following command:
python -m src.webhook_server


